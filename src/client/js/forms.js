const addTripHandler = async () => {
    // TODO: Make requests to all three services and gather information.
};

window.addEventListener('load', () => {
    const addTripButton = document.getElementById('add-trip');
    addTripButton.addEventListener('click', addTripHandler);
});
